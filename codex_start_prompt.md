# Codex 启动提示词

请你作为本项目的 Codex Agent，从现在开始负责实现项目代码。

在开始写代码前，请先完整阅读以下文档：

- agent.md
- docs/product_requirements.md
- docs/technical_implementation.md

如果这些文档实际放在项目根目录，请按项目中的实际路径读取。

## 项目核心范围

本项目 V1 是一个 Windows + Android 双端个人音乐同步播放器。

Windows 电脑端是主音乐库，负责导入音乐、播放音乐、创建和修改多个歌单，并开启 Wi-Fi 同步模式。

Android 手机端是只读随身播放器，负责扫码连接电脑端、按整个歌单同步音乐到手机本地、搜索和离线播放已同步音乐。

V1 使用 Flutter 全端开发。电脑端和手机端都使用 Flutter，并尽量共享 Dart 核心模块。

V1 只做 Wi-Fi 扫码同步，不做云同步、不做远程在线播放、不做 USB、不做蓝牙、不做歌词、不做封面、不做手机端编辑歌单、不做手机端导入音乐、不做手机端同步回电脑。

## 重要工作方式

请严格按 agent.md 中的阶段顺序推进，不要擅自扩展 V1 明确排除的功能。

你输出讲解时，不要写得过于专业。请用普通用户也能看懂的话说明：这次做了什么、怎么运行、怎么验证、还缺什么、下一步做什么。

如果需要安装依赖或准备环境，请直接告诉我：

1. 需要安装什么；
2. 这个东西是干什么的；
3. Windows 上怎么检查是否安装成功；
4. 不安装会影响哪一步。

如果当前环境无法生成 APK 或 exe，请如实说明原因，不要假装已经生成。

## 第一轮任务

第一轮请不要急着实现全部功能。请先完成项目初始化和基础骨架：

1. 创建 monorepo 项目目录结构；
2. 初始化 Windows Flutter APP；
3. 初始化 Android Flutter APP；
4. 创建共享包 packages/core、packages/database、packages/player、packages/sync_protocol；
5. 确保两个 APP 至少能启动到一个基础页面；
6. 加入最基础的 README 或运行说明；
7. 输出当前文件结构、运行方式、依赖要求、已知风险和下一步计划。

## 开始前必须先回复

在真正改代码前，请先复述你对 V1 核心范围的理解，并列出你发现的任何需求冲突、技术不确定点或环境依赖。

如果没有冲突，再开始创建项目结构。
