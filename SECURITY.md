# 安全说明

## 支持范围

当前只维护 `master` 分支上的最新 V1 代码。项目尚未提供应用商店签名版本；第三方重新打包的 APK 或 EXE 不在支持范围内。

## 同步安全边界

- 同步服务只应在可信的家庭或个人局域网内临时开启。
- 当前协议使用局域网 HTTP，不提供 TLS，也不适合端口映射、反向代理或公网暴露。
- 二维码包含本次会话的电脑地址、端口、临时会话 ID 和六位连接码。不要把正在使用的二维码公开分享。
- Android 会限制下载地址必须与已连接会话一致，并检查文件大小和 SHA-256，但这不等于可以信任陌生网络中的任意同步主机。
- 同步完成后应关闭 Windows 端同步模式，使临时会话失效。

## 私密报告漏洞

请使用仓库的 [GitHub Security Advisories](https://github.com/YZCore/music-sync-player/security/advisories/new) 私密报告安全问题。报告中请包含：

- 受影响的平台和提交版本。
- 可以稳定复现的最小步骤。
- 可能造成的数据、隐私或网络影响。
- 如果已经准备修复建议，可以一并说明。

在维护者确认并准备修复前，请不要公开连接信息、真实音乐路径、数据库内容或可直接利用的完整细节。
