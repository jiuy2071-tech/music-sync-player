# agent.md - Codex 项目执行指引

## 1. 项目身份

你是本项目的代码实现 Agent，负责根据既定需求逐步实现一个个人音乐同步播放器 V1。

本项目不是商业音乐平台，不是云音乐服务，不是远程在线播放器。它是一个面向个人使用的 Windows + Android 双端音乐同步播放器。

V1 的核心模式是：

电脑端 Windows APP 是主音乐库，负责导入、整理、播放和管理歌单。

手机端 Android APP 是只读随身播放器，负责同步、保存、搜索和播放已同步音乐。

同步方式只做同一 Wi-Fi 下扫码同步，方向为电脑端到手机端。

手机端同步完成后必须可以离线播放已同步音乐。

## 2. 已拍板的 V1 范围

### 2.1 平台

电脑端：Windows。

手机端：Android。

技术路线：Flutter 全端。

电脑端和手机端都使用 Flutter 开发，共享 Dart 核心模块。

V1 不考虑 macOS、iOS、Linux。

### 2.2 支持音频格式

V1 支持：

- MP3
- FLAC
- M4A
- WAV

其他格式暂不做 V1 必需支持。

### 2.3 导入规则

电脑端支持导入单个或多个音频文件。

电脑端支持导入整个文件夹，并扫描其中支持格式的音频文件。

导入时默认将音频复制进 APP 自己的音乐库目录，不只记录原路径。

数据库只保存歌曲信息和文件路径，不把音频二进制内容塞进数据库。

### 2.4 音频识别规则

导入音频时，优先读取音频内部元数据。

如果内部元数据缺失，再尝试使用文件名。

如果文件名有明确规律，例如“歌手 - 歌名”“歌名 - 歌手”，可以尝试识别。

如果文件名是数字、编码、乱码、无意义字符，不允许乱识别。

这类音频统一命名为：

- 未命名音频 001
- 未命名音频 002
- 未命名音频 003

依次递增，并进入“待整理音频”。

待整理音频可以播放、搜索、加入歌单、同步到手机。

V1 先不做在线歌曲识别，不做全网元数据匹配。

### 2.5 歌单规则

电脑端可以创建多个歌单。

电脑端可以重命名歌单、删除歌单、向歌单添加歌曲、从歌单移除歌曲。

手机端不能创建歌单，不能重命名歌单，不能删除歌单，不能修改歌单内容。

手机端只能查看、搜索、同步、播放电脑端同步过来的歌单。

手机端同步后的歌单永远以电脑端为准。

### 2.6 同步规则

V1 只做 Wi-Fi 同步。

电脑端每次需要手动打开“同步模式”。

电脑端打开同步模式后，生成二维码和临时连接码。

手机端点击“同步音乐”，扫码连接电脑端。

连接成功后，手机端显示电脑端歌单列表。

手机端选择整个歌单进行同步。

V1 同步粒度为整个歌单，不做歌单内部分歌曲选择，不做单曲同步。

同步方向为电脑端到手机端。

V1 不做手机端同步回电脑。

### 2.7 手机端存储与离线规则

手机端同步下来的音乐存入 APP 自己的本地目录。

手机端只显示已同步到本地的歌曲和歌单。

手机端离线时，只能播放已同步到本地的内容。

手机端可以删除本地缓存以释放空间。

删除本地缓存不影响电脑端主库，不影响电脑端歌单。

### 2.8 搜索规则

电脑端和手机端都要有搜索功能。

电脑端搜索范围：全部歌曲、歌单、待整理音频。

手机端搜索范围：已同步到本地的歌曲和歌单。

V1 做基础关键词搜索即可。

可搜索字段包括歌曲名、歌手、专辑、歌单名、未命名音频编号。

### 2.9 V1 不做内容

V1 不做：

- 云同步
- 公网远程播放
- 远程在线播放
- USB 同步
- 蓝牙同步
- 多用户登录
- 手机端编辑歌单
- 手机端导入音乐
- 手机端同步回电脑
- 歌词
- 封面
- 评论社交
- 推荐算法
- 在线歌曲识别
- 自动匹配全网歌曲信息
- 商业音乐平台接入

## 3. V1 成功标准

V1 只要跑通以下闭环，即视为成功：

1. 电脑端可以导入音乐文件或音乐文件夹。
2. 电脑端可以建立本地音乐库。
3. 电脑端可以播放本地音乐。
4. 电脑端可以创建多个歌单。
5. 电脑端可以把歌曲加入歌单。
6. 电脑端可以打开同步模式并显示二维码和连接码。
7. 手机端可以扫码连接电脑端。
8. 手机端可以看到电脑端歌单列表。
9. 手机端可以选择一个歌单同步。
10. 手机端可以下载该歌单内的音频文件到本地。
11. 手机端可以离线播放已同步歌曲。
12. 手机端可以搜索已同步歌曲和歌单。
13. 电脑端和手机端都能保持数据不乱、不重复、不破坏原始音乐文件。

## 4. 推荐工程结构

优先使用 monorepo 结构：

```text
music_sync_player/
  agent.md
  docs/
    product_requirements.md
    technical_implementation.md
  apps/
    windows_app/
    android_app/
  packages/
    core/
    database/
    player/
    sync_protocol/
  tools/
  README.md
```

### 4.1 apps/windows_app

Windows 电脑端 Flutter APP。

职责：

- 音频导入
- 音乐库管理
- 歌单管理
- 本地播放
- 搜索
- 同步模式
- 二维码和连接码展示
- 提供局域网同步服务

### 4.2 apps/android_app

Android 手机端 Flutter APP。

职责：

- 扫码连接电脑端
- 查看电脑端歌单列表
- 选择歌单同步
- 下载音频文件到 APP 本地目录
- 离线播放
- 搜索已同步内容
- 删除本地缓存

### 4.3 packages/core

共享核心模型。

应包括：

- Song
- Playlist
- PlaylistItem
- ImportResult
- SyncManifest
- SyncSession
- SyncDownloadTask
- AudioFormat
- SongMetadata
- AppError

### 4.4 packages/database

共享数据库访问层。

应包括：

- SQLite 初始化
- 数据表创建和迁移
- SongRepository
- PlaylistRepository
- SyncRepository
- SearchRepository

### 4.5 packages/player

播放器封装。

应屏蔽底层播放器库差异，让 Windows 端和 Android 端尽量复用播放逻辑。

V1 至少支持：

- 播放本地文件
- 暂停
- 继续
- 上一首
- 下一首
- 当前进度
- 播放队列

### 4.6 packages/sync_protocol

同步协议模块。

应包括：

- 二维码载荷格式
- 连接码验证
- 歌单清单接口模型
- 歌曲清单接口模型
- 文件下载任务模型
- 同步进度模型
- 错误码模型

## 5. 推荐数据模型

V1 可以先使用 SQLite。

### 5.1 songs

建议字段：

- id：歌曲唯一 ID
- title：歌曲名
- artist：歌手，未知时使用“未知歌手”
- album：专辑，未知时使用“未知专辑”
- duration_ms：时长
- format：音频格式
- file_size：文件大小
- file_hash：文件 hash，用于去重和同步判断
- local_path：本地文件路径
- original_file_name：原始文件名
- display_name_source：显示名称来源，例如 metadata、filename、unnamed
- is_pending_review：是否待整理
- created_at：导入时间
- updated_at：更新时间

### 5.2 playlists

建议字段：

- id：歌单唯一 ID
- name：歌单名称
- sort_order：排序
- created_at：创建时间
- updated_at：更新时间

### 5.3 playlist_items

建议字段：

- id：关系 ID
- playlist_id：歌单 ID
- song_id：歌曲 ID
- sort_order：歌单内排序
- created_at：加入时间

### 5.4 sync_state

建议字段：

- id
- song_id
- playlist_id
- synced_at
- sync_version
- local_cache_path
- status

手机端和电脑端都可以有 sync_state，但含义不同。

电脑端用于生成同步清单。

手机端用于记录哪些歌曲已经下载到本地。

## 6. 推荐目录结构

### 6.1 Windows 端音乐库目录

默认结构可以设计为：

```text
UserSelectedMusicLibrary/
  audio/
    <song_id>.<ext>
  library.db
  config.json
```

V1 不做封面，所以不需要 covers 目录。

### 6.2 Android 端本地目录

手机端使用 APP 自己的本地目录：

```text
app_documents/
  audio/
    <song_id>.<ext>
  library.db
  config.json
```

手机端不得依赖原始文件名进行播放和同步。

## 7. Wi-Fi 同步流程

### 7.1 电脑端开启同步模式

电脑端执行：

1. 检测本机局域网 IP。
2. 启动本地 HTTP 服务。
3. 生成临时连接码。
4. 生成同步 session_id。
5. 生成二维码。
6. 在界面显示二维码和连接码。

### 7.2 二维码载荷

二维码内容建议使用 JSON 或 URL。

推荐 JSON：

```json
{
  "app": "personal_music_sync",
  "version": 1,
  "host": "192.168.1.10",
  "port": 37891,
  "session_id": "temporary-session-id",
  "connect_code": "123456"
}
```

不要在二维码中放长期密钥。

连接码只对当前同步模式有效。

### 7.3 手机端连接电脑端

手机端扫码后：

1. 解析二维码。
2. 请求电脑端连接验证接口。
3. 提交 session_id 和 connect_code。
4. 验证成功后获取当前可同步歌单列表。

### 7.4 手机端同步歌单

手机端选择一个歌单后：

1. 请求该歌单的同步清单。
2. 对比手机端本地数据库。
3. 跳过已存在且 hash 一致的歌曲。
4. 下载缺失或版本变化的歌曲。
5. 写入手机端本地 audio 目录。
6. 更新手机端 library.db。
7. 更新歌单关系。
8. 同步完成后允许离线播放。

### 7.5 同步失败处理

V1 需要基础失败处理：

- 单首下载失败时标记失败，不要导致整个数据库损坏。
- 已成功下载的文件保留。
- 用户可以重新同步同一歌单。
- 重新同步时跳过已成功且 hash 一致的歌曲。

## 8. 实现顺序

必须按以下顺序推进，不要一开始做大而全。

### Phase 1：工程初始化

1. 创建 monorepo。
2. 创建 Windows Flutter APP。
3. 创建 Android Flutter APP。
4. 创建 packages/core。
5. 创建 packages/database。
6. 创建 packages/player。
7. 创建 packages/sync_protocol。
8. 保证两个 APP 能启动。

### Phase 2：核心数据模型和数据库

1. 实现 Song、Playlist、PlaylistItem 模型。
2. 实现 SQLite 初始化。
3. 实现 songs、playlists、playlist_items 表。
4. 实现基础 CRUD。
5. 实现搜索查询。

### Phase 3：电脑端导入音乐

1. 支持选择文件导入。
2. 支持选择文件夹导入。
3. 只接受 MP3、FLAC、M4A、WAV。
4. 导入时复制到音乐库 audio 目录。
5. 尝试读取元数据。
6. 文件名有规律时尝试识别。
7. 数字、编码、乱码文件名按“未命名音频 001”规则处理。
8. 写入数据库。
9. 显示导入结果。

### Phase 4：电脑端歌单和播放

1. 实现全部歌曲列表。
2. 实现待整理音频列表。
3. 实现搜索。
4. 实现创建歌单。
5. 实现重命名歌单。
6. 实现删除歌单。
7. 实现加入歌单。
8. 实现从歌单移除歌曲。
9. 实现本地播放。

### Phase 5：同步模式

1. 电脑端启动本地 HTTP 服务。
2. 生成 session_id 和 connect_code。
3. 生成二维码。
4. 实现连接验证接口。
5. 实现歌单列表接口。
6. 实现歌单同步清单接口。
7. 实现音频文件下载接口。

### Phase 6：手机端同步和本地播放

1. 手机端扫码。
2. 手机端连接电脑端。
3. 手机端展示电脑端歌单。
4. 手机端选择整个歌单同步。
5. 下载音频到 APP 本地目录。
6. 写入手机端数据库。
7. 展示已同步歌单。
8. 展示已同步歌曲。
9. 实现手机端搜索。
10. 实现离线播放。
11. 实现删除本地缓存。

### Phase 7：验收和修复

1. 用真实 MP3 测试。
2. 用 FLAC 测试。
3. 用 M4A 测试。
4. 用 WAV 测试。
5. 用数字文件名测试。
6. 用乱码文件名测试。
7. 用重复文件测试。
8. 用同步中断测试。
9. 用离线播放测试。
10. 用重新同步测试。

## 9. 第一性原理

第一性原理是本项目的强制执行原则。Agent 在实现任何功能前，都必须先回到项目最基础的事实、目标和约束，而不是照搬常见音乐软件、流媒体服务器或通用文件同步工具的做法。

本项目的第一性事实是：

1. 用户只有一个人，V1 不面向公开上线和多用户商业场景。
2. Windows 端是主音乐库，Android 端是只读随身库。
3. 手机端不是远程播放器，必须能离线播放已同步音乐。
4. V1 只做同一 Wi-Fi 下扫码同步，不做云同步、远程在线播放、USB、蓝牙。
5. 同步粒度是整个歌单，手机端不编辑歌单。
6. 导入音频默认复制进 APP 音乐库目录，不能破坏用户原始文件。
7. 文件名可能是数字、编码或乱码，不能依赖文件名乱识别歌曲。
8. V1 成功标准是“电脑端导入音乐—建歌单—扫码同步—手机端离线播放”闭环跑通。

所有实现决策都必须服务这个闭环。遇到复杂选择时，优先选择更简单、更稳定、更可验证、更不破坏数据的方案。

第一性原理下的执行要求：

1. 先保证本地导入、建歌单、同步、离线播放闭环，再优化体验。
2. 先保证数据安全和可恢复，再追求自动化。
3. 先保证手机端已同步内容能稳定播放，再考虑更多播放器功能。
4. 先保证电脑端主库权威，再考虑手机端交互增强。
5. 先做 V1 明确需要的功能，不因为“音乐软件通常都有”而擅自扩展。
6. 不确定的平台 API、依赖用法或构建限制，必须标记风险并用最小验证确认，不能假装确定。
7. 不确定的音频元数据不要乱识别。
8. 不确定的错误处理必须返回明确错误，而不是静默失败。

尤其注意：

- 不要假设文件名就是歌曲名。
- 不要假设所有音频都有元数据。
- 不要假设手机和电脑永远在同一网络。
- 不要假设下载不会中断。
- 不要假设同名文件就是同一首歌。
- 不要假设手机端可以修改主库。
- 不要假设未来会做云同步。

如果需求文档没有明确要求某个功能，不要主动扩展。

如果你认为必须扩展，先写成建议，不要直接实现。

## 10. 对抗性审查

每完成一个模块后，都要进行对抗性审查。

对抗性审查不是挑刺，而是主动假设实现会在真实使用中出错，然后提前修正。

### 10.1 导入模块审查

检查：

- 数字文件名是否被误认为歌曲名？
- 乱码文件名是否进入待整理音频？
- 重复文件是否被重复导入？
- 导入失败是否影响已成功导入文件？
- 原始文件是否被误删或移动？
- 不支持格式是否被清楚跳过？

### 10.2 歌单模块审查

检查：

- 同一首歌能否加入多个歌单？
- 删除歌单是否误删歌曲文件？
- 从歌单移除歌曲是否误删主库歌曲？
- 歌单排序和歌曲排序是否稳定？
- 手机端是否完全没有修改歌单入口？

### 10.3 同步模块审查

检查：

- 电脑端未开启同步模式时，手机是否无法连接？
- 旧连接码是否失效？
- 同一 Wi-Fi 下其他设备是否能绕过连接码？
- 下载中断后是否能重新同步？
- 已下载且 hash 一致的文件是否会被重复下载？
- 手机端歌单是否永远以电脑端为准？
- 手机端是否只显示已同步内容？

### 10.4 播放模块审查

检查：

- 本地文件缺失时是否有清楚提示？
- 手机断网后是否仍能播放已同步歌曲？
- 删除缓存后是否不会播放不存在的文件？
- 播放队列切歌是否稳定？

### 10.5 数据库审查

检查：

- 数据表是否有必要索引？
- 删除操作是否误删文件？
- 歌曲 ID 是否稳定？
- 文件 hash 是否用于去重和同步？
- 搜索是否只搜索本端允许的数据范围？

## 11. 开发纪律

1. 先跑通闭环，再优化界面。
2. 先实现最小功能，再做体验增强。
3. 不做 V1 明确排除的功能。
4. 每次改动保持可运行。
5. 重要文件操作必须有错误处理。
6. 同步相关代码必须可重复执行。
7. 手机端不得拥有修改电脑端歌单的逻辑。
8. 手机端不得展示未同步到本地的歌曲。
9. 不要破坏用户原始音频文件。
10. 不要为了“像音乐软件”而擅自加入歌词、封面、推荐、云同步。

## 12. 给 Codex 的默认工作方式

你应按阶段执行任务。

每次只实现一个明确模块。

实现前先阅读 docs/product_requirements.md 和 docs/technical_implementation.md。

实现时优先保证业务规则正确。

实现后给出：

- 改动文件列表
- 实现内容摘要
- 如何运行
- 如何测试
- 已知风险
- 下一步建议

如果发现文档和代码冲突，以本文档和产品需求文档为准。

如果发现需求内部冲突，停止扩展实现，返回冲突说明。

## 13. 面向用户的输出风格和依赖告知

Codex/Agent 的回复对象是项目发起者本人，不是专业软件工程团队。

因此，输出说明必须清楚、直白、可操作，不要堆大量专业术语。必要术语可以使用，但必须用一句普通话解释它的作用。

每次阶段性回复应优先说明：

- 这次完成了什么；
- 用户可以怎么运行或验证；
- 还缺什么；
- 下一步做什么；
- 有没有需要用户安装的东西。

不得只输出“已完成”“请运行测试”这类空话。

如果需要用户安装依赖或准备环境，必须明确告诉用户：

- 需要安装什么；
- 这个东西是用来干什么的；
- Windows 上大概从哪里安装；
- 安装完成后用什么命令检查是否成功；
- 如果暂时不安装，会影响哪一步。

常见需要告知的依赖包括但不限于：

- Flutter SDK；
- Dart SDK，通常随 Flutter 一起提供；
- Android Studio；
- Android SDK；
- Android 手机 USB 调试，若需要真机调试；
- Windows 桌面构建所需环境；
- 项目依赖包，例如音频播放、SQLite、二维码、扫码、文件选择、HTTP 服务等 Flutter/Dart 依赖。

依赖说明要写给普通用户看。例如，不要只说“安装 Android toolchain”，应说明“这是用来把手机端 APP 打包成 APK 的工具链”。

当输出代码改动说明时，使用“普通解释 + 必要文件名”的方式，不要写成过度专业的架构论文。

推荐说明格式：

```text
本次完成：
1. 创建了 Windows 端入口。
2. 创建了 Android 端入口。
3. 增加了共享的数据模型。

你需要安装/确认：
1. Flutter SDK：用来运行和打包两个 APP。
2. Android Studio：用来准备 Android APK 构建环境。

你可以这样验证：
1. 运行 flutter doctor。
2. 运行 flutter run -d windows。

当前限制：
1. 还没有实现导入音乐。
2. 还没有实现同步。
```

如果当前环境无法构建 APK 或 Windows exe，必须如实说明，不能假装已经生成。

## 14. V1 最终交付物要求

Codex/Agent 不能只提交源码。V1 的最终交付必须包含可运行产物，方便用户直接试用。

### 14.1 Android 端交付物

必须生成 Android APK 安装包。

APK 目标：用户可以把 APK 下载到 Android 手机上，并通过系统安装流程安装手机端 APP。

V1 允许交付 debug APK 或 release APK，但必须在交付说明中明确 APK 类型、文件路径、安装方式和可能的 Android 安装限制。

推荐最终输出路径示例：

```text
release/android/music_sync_player_v1.apk
```

如果使用 Flutter 标准构建流程，优先保证以下命令可运行：

```bash
flutter build apk
```

构建完成后，应将生成的 APK 复制或说明到清晰的 release 目录中，避免用户去复杂的 build 子目录里寻找。

### 14.2 Windows 端交付物

必须生成 Windows 桌面端可启动程序。

用户期望的使用方式是：在 Windows 上双击 exe 文件，即可打开电脑端 APP。

推荐最终输出路径示例：

```text
release/windows/MusicSyncPlayer/MusicSyncPlayer.exe
```

Windows 端不能只要求用户通过命令行 `flutter run` 启动。

如果使用 Flutter 标准构建流程，优先保证以下命令可运行：

```bash
flutter build windows
```

构建完成后，应整理 Windows 运行所需的 exe 和伴随动态库/数据文件。不要只复制单个 exe 而遗漏 Flutter Windows 运行所需文件。

### 14.3 交付说明

最终必须提供 `RELEASE_NOTES.md` 或等价说明，至少包含：

- Android APK 文件路径；
- Windows exe 文件路径；
- Windows 端如何启动；
- Android 端如何安装；
- 两端如何处于同一 Wi-Fi 并完成扫码同步；
- 已知限制；
- 如果构建失败，失败原因和未完成项。

### 14.4 构建真实性要求

如果 Agent 无法在当前环境实际生成 APK 或 exe，必须遵循第一性原理和构建真实性要求，不能假装已经生成。必须明确说明：

- 哪一步无法执行；
- 缺少什么环境或依赖；
- 本地应执行哪些命令生成产物；
- 当前源码是否已经准备好构建。

